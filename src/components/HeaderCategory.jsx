const HeaderCategory = ({title}) =>{
      return(
            <li className="text-white cursor-pointer uppercase text-xs sm:text-sm font-semibold">{title}</li>
      )
}

export default HeaderCategory